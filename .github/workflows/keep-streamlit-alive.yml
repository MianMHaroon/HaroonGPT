name: Keep Streamlit App Alive

on:
  schedule:
    - cron: "*/30 * * * *"   # every 30 minutes
  workflow_dispatch:

jobs:
  ping-streamlit:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: |
          curl -s https://haroongpt.streamlit.app/ > /dev/null
          echo "✅ Streamlit app pinged successfully"
